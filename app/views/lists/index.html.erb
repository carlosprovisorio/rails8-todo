<div class="d-flex align-items-center justify-content-between mb-3">
  <h1 class="h4 mb-0">Your Lists</h1>
</div>

<%= turbo_frame_tag "lists" do %>
  <%= render "index_body", lists: @lists, list: @list %>
<% end %>
